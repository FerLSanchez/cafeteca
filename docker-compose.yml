services:
  cafeteca:
    image: ghcr.io/ferlsanchez/cafeteca:latest
    build: .
    ports:
      - "127.0.0.1:5323:5323"  # only NPM (Authelia gate) may reach it — see CLAUDE.md
    volumes:
      - ./data:/data
    restart: unless-stopped
